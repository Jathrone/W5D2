

<a href="<%= comment_url(comment)%>"><%= comment.content%></a>
<%= comment.author.username %>
<span style="color: gray; font-size:6px;"><%= comment.created_at %></span>
    <ul>

    <% all_comments.each do |c2|%>
        <%if c2.parent_comment_id == comment.id %>
            <li><%= render partial: "comments/faster_comment", locals: {all_comments:all_comments, comment: c2}  %></li>
        <%end%>
    <% end %>

</ul>